<%= form_for(@visibility) do |f| %>
  <% if @visibility.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@visibility.errors.count, "error") %> prohibited this visibility from being saved:</h2>

      <ul>
      <% @visibility.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :type %><br>
    <%= f.text_field :vtype %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
