<%= form_for(@htype) do |f| %>
  <% if @htype.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@htype.errors.count, "error") %> prohibited this htype from being saved:</h2>

      <ul>
      <% @htype.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
