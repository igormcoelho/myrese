<%= form_for(@pkeyword) do |f| %>
  <% if @pkeyword.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@pkeyword.errors.count, "error") %> prohibited this pkeyword from being saved:</h2>

      <ul>
      <% @pkeyword.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :publication_id %><br>
    <%= f.text_field :publication_id %>
  </div>
  <div class="field">
    <%= f.label :infohash_id %><br>
    <%= f.text_field :infohash_id %>
  </div>
  <div class="field">
    <%= f.label :keyword %><br>
    <%= f.text_field :keyword %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
